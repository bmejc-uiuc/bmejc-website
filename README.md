Opening react page while editing: npm start
1. Pulling - do this before commiting so you have the most up-to-date code, it will pull all updates from all files: git pull
2. Commiting: git commit -m "insert message here"
3. Pushing - pushes code to github, always do this after committing: git push

